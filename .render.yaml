services:
  - type: web
    name: daily-diet-api
    env: python
    plan: free
    branch: main
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python src/app.py"
    pythonVersion: 3.11.9